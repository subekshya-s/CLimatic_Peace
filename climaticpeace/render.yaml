services:
  - type: web
    name: climaticpeace
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn climaticpeace.wsgi:application"
    envVars:
      - key: DEBUG
        value: False
